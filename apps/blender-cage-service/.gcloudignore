# Files NOT to upload during `gcloud builds submit`.
# The .blend template MUST be uploaded (the Docker COPY needs it),
# everything else is local-only.

.git
.gitignore
*.pyc
__pycache__/
.DS_Store
README.md
